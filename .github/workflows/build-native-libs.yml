name: Build Native Libraries

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  build-macos:
    name: Build macOS dylib
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Build frost-ffi for macOS
        run: cargo build --release -p frost-ffi

      - name: Upload macOS dylib
        uses: actions/upload-artifact@v4
        with:
          name: libfrost_ffi-macos
          path: target/release/libfrost_ffi.dylib

  build-linux:
    name: Build Linux .so
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Build frost-ffi for Linux
        run: cargo build --release -p frost-ffi

      - name: Upload Linux .so
        uses: actions/upload-artifact@v4
        with:
          name: libfrost_ffi-linux
          path: target/release/libfrost_ffi.so